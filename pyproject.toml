[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "football-ml"
version = "0.1.0"
description = "Football match outcome modeling and prediction"
authors = [{name = "AutoGenerated"}]
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "pandas",
    "numpy",
    "requests",
    "scikit-learn",
    "xgboost",
    "joblib",
    "python-dotenv",
]

[project.optional-dependencies]
dev = ["pytest"]

[tool.setuptools]
include-package-data = true
packages = { find = {} }
